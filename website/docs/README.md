# Paperwise Docs

This directory contains the Starlight-powered docs site for Paperwise and lives inside the
main `website/` directory so docs can ship as part of the public site.

## Local development

```bash
cd website/docs
npm install
npm run dev
```

## Build

```bash
npm run build
```

The docs content lives in `src/content/docs`.
